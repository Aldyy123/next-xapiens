export interface LoginValues {
    email: string;
    password: string;
}

export interface IUser {
    id: string;
    email: string;
    avatar: string;
    first_name: string;
    last_name: string;
}